fix(fleet): address second-round PR #176 review feedback

P1 fixes:
- Compose bundled-PG password: pass FLEET_POSTGRES_PASSWORD as separate
  env var, use single $ in DATABASE_URL for proper interpolation
- Compose external-PG deadlock: add required: false to fleet-postgres
  dependency so external PG users do not need bundled PG to be healthy
- Kustomize port: change OCTO_APP_SERVER_URL from :8090 to default port
  (kustomize exposes octo-server on ClusterIP port 80)
- Helm sslMode: change default from require to disable (consistent with
  Compose/Kustomize, most self-hosted PG has no TLS)

Documentation sync:
- Update README.md + README.zh.md with correct OCTO_APP_SERVER_URL port
- Update README.md + README.zh.md with correct ALLOW_SIGNUP default

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
